# Angry-box

SSH orchestrator for amnezia-box / sing-box. It does not route traffic itself.
UI is Go Templ + HTMX. State is the encrypted store.

**Do not read this file as the operating manual.** Read `.agents/00-index.md`.

Session default: `.agents/00-index.md` → `01-purpose.md` + `05-rules.md`.
Everything else on demand. `docs/PROGRESS.md`: last 1–2 entries only.

Frozen for new configs: TUIC, Hysteria2. Prod GCP nodes: do not touch.
